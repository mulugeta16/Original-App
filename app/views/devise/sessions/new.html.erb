<div class="container align-items-center">
  <div class ="card m-5 p-5 text-light" style="background-color: #0e47a1; width:40%;">
  <h2>Log in</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email",class:'form-control text-white', placeholder:'Enter your email' %>
    </div>

    <div class="field">
      <%= f.label :password %><br />
      <%= f.password_field :password, autocomplete: "current-password", class:'form-control text-white', placeholder:'FIll your password' %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>
    
    <div class="actions">
      <%= f.submit "Log in", class:'btn btn-dark btn-sm' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |g| %>
    <%= g.hidden_field :email, value: 'gues1t@example.com' %>
    <%= g.hidden_field :password, value: '12345678' %>
    <div class="mb-3">
      <%= g.submit 'Guest login', class: 'btn btn-warning' %>
     </div>
  <% end %>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |g| %>
        <%= g.hidden_field :email, value: 'admin@gmail.com' %>
        <%= g.hidden_field :password, value: '123456' %>
        <div class="mb-3">
          <%= g.submit 'Admin_Guestlogin', class: 'btn btn-warning' %>
         </div>
      <% end %>
  </div>
</div>  